import { SpinnerContainer } from "./Spinner.styled";

function Spinner() {
  return(
    <SpinnerContainer>
      <span className="loader"></span>
    </SpinnerContainer>
  )
}

export default Spinner;