import styled from 'styled-components';

export const AuthLabeledContainer = styled.div`
  width: max-content;
  height: 1.5rem;
  display: flex;
  position: relative;
  display: flex;
  align-items: center;
  gap: 3px;

  #auth-labeled-str {
    width: max-content;
    height: 20px;
    line-height: 19px;
    font-size: 14px;
    font-weight: 700;
    color: #21b1a0;
  }

  #auth-mark-icon {
    width: 1rem;
    height: 1rem;
  }
`;
